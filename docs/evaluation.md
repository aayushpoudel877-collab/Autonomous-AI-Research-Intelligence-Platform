# Evaluation

The project separates retrieval evaluation from generation evaluation. `recall_at_k` is the initial retrieval metric. Future evaluation should include citation precision, groundedness, contradiction detection, calibration, multilingual retrieval, latency, cost, and regression suites over frozen evidence corpora.
